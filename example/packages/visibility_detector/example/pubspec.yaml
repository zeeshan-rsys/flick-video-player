name: visibility_detector_example

publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  visibility_detector:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
